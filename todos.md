# TODOs

- Wire MCP ingestion servers into CI smoke tests without hitting external APIs.
- Add persistence layer for checkpointing offsets to disk or database.
- Map full schema coverage (30+ tables) for each provider into configuration files.
- Generate SQL migration scripts that align with normalized ingestion outputs.
