# Example environment variables - DO NOT PUT SECRETS HERE
# Copy this file to .env and fill in secure values locally

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=openleg
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Elasticsearch
ES_HOST=localhost
ES_PORT=9200

# API keys
GOVINFO_API_KEY=your_govinfo_api_key
# Local environment variables for OpenLegislation (DO NOT commit secrets)
# Copy to `.env` and edit values for your machine.
# Sample values are placeholders — replace before use.

# Postgres connection
PGHOST=your-db-host
PGPORT=5432
PGUSER=your-db-user
PGPASSWORD=your-db-password
PGDATABASE=your-db-name

# Optional JDBC url convenience (used by some launchers)
# Example: jdbc:postgresql://localhost:5432/openleg
JDBC_DATABASE_URL=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}

# Optional: test DB for tools/test scripts
TEST_DB_HOST=your-db-host
TEST_DB_PORT=5432
TEST_DB_NAME=your-test-db-name
TEST_DB_USER=your-test-db-user
TEST_DB_PASSWORD=your-test-db-password

# API keys / secrets
# Replace values with your actual keys when configuring local `.env`.
LRS_API_KEY=your_api_key_here
SOME_OTHER_SECRET=your_secret_here

# DO NOT commit this file with real credentials. Keep secrets out of VCS.
