# Staging directory for real GovInfo calendar data
# This directory should contain actual GovInfo calendar JSON files with names like CALENDAR-YYYY.json
# Files should be downloaded from GovInfo using the bulk download scripts